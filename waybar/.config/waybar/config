{
    "layer": "top",
    "position": "top",  // or "bottom", "left", "right"
    "exclusive": true,  // set to false if you want to hide bar
    "spacing": 0,
    "margin-left": 0,
    "margin-top": 0,
    "margin-right": 0,
    "sort-by-number": true,

    "modules-left": [
        "clock",
        "clock#date"
    ],
    "modules-center": [
    ],
    "modules-right": [
        "custom/vpn",
        "backlight",
        "pulseaudio",
        "bluetooth",
        "network",
        "battery"
    ],

    "bluetooth": {
        "on-click": "ghostty -e bluetui",
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-icons": {
            "headphone": "",
            "default": ["","",""]
        },
        "on-click": "pavucontrol",
        "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +1%",
        "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -1%"
    },

    "backlight": {
        "format": "{icon} {percent}%",
        "format-icons": ["", ""],
        "on-click": "brightnessctl 20%",
        "on-click-right": "brightnessctl 80%",
        "on-scroll-up": "brightnessctl +1%",
        "on-scroll-down": "brightnessctl 1%-"
    },

    "network": {
        "interface": "wlp1s0",
        "format": "{ifname}",
        "format-wifi": "<span>  </span>{essid}",
        "format-ethernet": "{ipaddr}/{cidr} ",
        "format-disconnected": "<span></span> Offline",
        "on-click": "ghostty -e nmtui",
        "tooltip": false,
    },

    "custom/vpn": {
        "exec": "~/.dotfiles/scripts/vpnst",
        "return-type": "json",
        "interval": 5,
        "tooltip": false,
        "on-click": "ghostty -e ~/.dotfiles/scripts/vpntog"
    },

    "battery": {
        "format": "<span>{icon}</span> {capacity}%",
        "format-icons": ["", "", "", "", "", "", "", "", "", ""],
        "format-charging": "<span></span> {capacity}%",
        "tooltip": false,
    },

    "clock": {
        "format": "󰥔 {:%I:%M%p}",
        "height": 26
    },
    "clock#date": {
        "format": " {:%A, %b %d}"
    },
}
}

